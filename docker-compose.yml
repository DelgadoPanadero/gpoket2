services:
  sandbox:
    tty: True
    build:
      context: ./
      args:
        USERNAME: ${USER:-vscode}
        USER_UID: ${UID:-1000}
    volumes:
      - ./:/workspace
    environment:
      S3_ENDPOINT: http://minio:9000
      S3_ACCESS_KEY: minioadmin
      S3_SECRET_KEY: minioadmin